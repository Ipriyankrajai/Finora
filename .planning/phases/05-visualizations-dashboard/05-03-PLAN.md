---
phase: 05-visualizations-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/api/src/routers/dashboard.ts
  - apps/web/src/lib/loan-calculations.ts
  - apps/web/src/components/ui/slider.tsx
  - apps/web/src/components/dashboard/loan-overview-card.tsx
  - apps/web/src/components/dashboard/loan-amortization-chart.tsx
  - apps/web/src/components/dashboard/what-if-simulator.tsx
  - apps/web/src/components/dashboard/quick-add-fab.tsx
  - apps/web/src/components/dashboard/dashboard-page-client.tsx
autonomous: true

must_haves:
  truths:
    - "User can see loan cards with balance and payoff date on dashboard"
    - "User can see loan amortization chart showing balance over time"
    - "User can simulate extra payment and see updated payoff date in real-time"
    - "User can quick-add a transaction from the dashboard via FAB"
  artifacts:
    - path: "apps/web/src/lib/loan-calculations.ts"
      provides: "Client-side what-if projection"
      exports: ["projectPayoffWithExtra"]
    - path: "apps/web/src/components/dashboard/what-if-simulator.tsx"
      provides: "Slider + payoff display with comparison"
      min_lines: 80
    - path: "apps/web/src/components/dashboard/quick-add-fab.tsx"
      provides: "Floating action button for quick transaction add"
      min_lines: 30
    - path: "apps/web/src/components/dashboard/loan-amortization-chart.tsx"
      provides: "Area chart showing loan balance over time"
      min_lines: 60
  key_links:
    - from: "apps/web/src/components/dashboard/what-if-simulator.tsx"
      to: "projectPayoffWithExtra"
      via: "import and call"
      pattern: "projectPayoffWithExtra"
    - from: "apps/web/src/components/dashboard/what-if-simulator.tsx"
      to: "useDeferredValue"
      via: "React concurrent rendering"
      pattern: "useDeferredValue"
    - from: "apps/web/src/components/dashboard/quick-add-fab.tsx"
      to: "TransactionForm"
      via: "dialog/modal"
      pattern: "TransactionForm"
---

<objective>
Complete the dashboard with loan visualizations, what-if simulator, and quick-add transaction capability.

Purpose: Enables users to see their loan progress visually (VIZ-03), simulate extra payments to understand impact (VIZ-04), view loan cards (DASH-03), and quickly add transactions (DASH-04). The what-if simulator is key to Finora's value proposition: "see exactly when loans will be paid off."

Output: Loan overview cards, amortization chart, interactive what-if simulator with real-time payoff calculations, and FAB for quick transaction entry.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-visualizations-dashboard/05-CONTEXT.md
@.planning/phases/05-visualizations-dashboard/05-RESEARCH.md

# Prior plan output
@.planning/phases/05-visualizations-dashboard/05-01-SUMMARY.md

# Key existing files
@packages/api/src/lib/calculations.ts
@packages/api/src/routers/dashboard.ts
@apps/web/src/hooks/use-dashboard.ts
@apps/web/src/components/transactions/transaction-form.tsx
@apps/web/src/components/ui/dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loan calculation utilities and slider component</name>
  <files>
    apps/web/src/lib/loan-calculations.ts
    apps/web/src/components/ui/slider.tsx
  </files>
  <action>
1. Create `apps/web/src/lib/loan-calculations.ts`:
   - Port the projectPayoff logic from packages/api/src/lib/calculations.ts for client-side use
   - Export `projectPayoffWithExtra` function:
     ```typescript
     interface PayoffProjection {
       monthsRemaining: number;
       totalInterestCents: bigint;
       payoffDate: Date;
     }

     interface WhatIfResult extends PayoffProjection {
       monthsSaved: number;
       interestSavedCents: bigint;
     }

     export function projectPayoffWithExtra(
       balanceCents: bigint,
       annualRatePercent: number,
       monthlyPaymentCents: bigint,
       extraPaymentCents: bigint
     ): WhatIfResult
     ```
   - Implementation: call internal projectPayoff twice (with and without extra) and compute difference
   - Handle edge cases: zero balance (already paid off), payment doesn't cover interest (Infinity)

2. Create `apps/web/src/components/ui/slider.tsx`:
   - Build on Base UI primitives or create simple controlled slider
   - Props: `{ value, onChange, min, max, step, disabled? }`
   - Accessible: proper aria labels, keyboard navigation
   - Style with Tailwind: track, thumb, active state
   - Use HTML range input with custom styling (simplest approach)
   - Format pattern:
     ```typescript
     interface SliderProps {
       value: number;
       onChange: (value: number) => void;
       min: number;
       max: number;
       step: number;
       disabled?: boolean;
       label?: string;
       formatValue?: (value: number) => string;
     }
     ```
  </action>
  <verify>
    - `cd apps/web && bun run typecheck` passes
    - projectPayoffWithExtra returns correct monthsSaved and interestSavedCents
  </verify>
  <done>
    - Client-side loan projection utility works correctly
    - Slider component renders and responds to input
    - Keyboard navigation works on slider
  </done>
</task>

<task type="auto">
  <name>Task 2: Create loan overview cards and amortization chart</name>
  <files>
    packages/api/src/routers/dashboard.ts
    apps/web/src/components/dashboard/loan-overview-card.tsx
    apps/web/src/components/dashboard/loan-amortization-chart.tsx
  </files>
  <action>
1. Add `getAmortizationSchedule` procedure to `packages/api/src/routers/dashboard.ts`:
   - Input: `{ loanId: string }`
   - Fetch loan by ID with authorization check (userId match)
   - Generate amortization schedule using loop:
     - Start from current balance
     - Each month: calculate interest, subtract payment, record balance
     - Continue until balance <= 0 or max 360 months
   - Return array: `{ month: Date, balanceCents: bigint }`
   - Cap at 360 points (30 years) per RESEARCH.md

2. Create `apps/web/src/components/dashboard/loan-overview-card.tsx`:
   - Accept loan data from loanOverview array
   - Display: loan name, current balance, payoff date, monthly payment
   - Format all money with formatCents
   - Handle infinite payoff: show "N/A" with "Increase payment" hint (per decision 04-03)
   - Style as Card with consistent spacing
   - Make card clickable to expand what-if simulator for that loan
   - Visual: progress indicator showing % paid off (principal paid / original principal)

3. Create `apps/web/src/components/dashboard/loan-amortization-chart.tsx`:
   - Accept `{ loanId, loanName }` props
   - Fetch amortization data using trpc.dashboard.getAmortizationSchedule
   - Use Recharts AreaChart:
     - X-axis: month (formatted as "MMM 'YY")
     - Y-axis: balance in dollars (with $ formatter)
     - Area fill with gradient
   - Convert BigInt to number at chart boundary
   - Handle loading state with skeleton
   - Responsive with explicit height container
   - Show total months/years to payoff in chart header
  </action>
  <verify>
    - `cd packages/api && bun run typecheck` passes
    - `cd apps/web && bun run typecheck` passes
    - getAmortizationSchedule returns valid schedule for test loan
  </verify>
  <done>
    - API endpoint returns amortization schedule for specified loan
    - Loan cards display balance, payoff date, progress
    - Amortization chart renders balance over time
    - VIZ-03 and DASH-03 requirements met
  </done>
</task>

<task type="auto">
  <name>Task 3: Create what-if simulator and quick-add FAB</name>
  <files>
    apps/web/src/components/dashboard/what-if-simulator.tsx
    apps/web/src/components/dashboard/quick-add-fab.tsx
    apps/web/src/components/dashboard/dashboard-page-client.tsx
  </files>
  <action>
1. Create `apps/web/src/components/dashboard/what-if-simulator.tsx`:
   - Props: loan data (balanceCents, annualRatePercent, monthlyPaymentCents, name)
   - State: extraPayment (number in dollars)
   - Use useDeferredValue for extraPayment to keep slider responsive (per RESEARCH.md)
   - Use useMemo to calculate projection with deferred value
   - Display current baseline vs with-extra comparison:
     - Original payoff date vs new payoff date
     - Months saved
     - Interest saved (formatted as currency)
   - Slider range: $0 to $1000 (or reasonable max based on monthly payment)
   - Slider step: $25 increments
   - Show visual indicator when calculation is pending (isPending = extra !== deferredExtra)
   - Full breakdown per CONTEXT.md: "payoff date, time saved, interest saved, comparison chart"
   - Optional: small comparison chart showing both scenarios

2. Create `apps/web/src/components/dashboard/quick-add-fab.tsx`:
   - Fixed position: bottom-right corner (`fixed bottom-6 right-6`)
   - Circular button with Plus icon
   - Accessible: aria-label="Add transaction"
   - On click: open Dialog with TransactionForm
   - On success: close dialog, show toast
   - Reuse existing TransactionForm component from @/components/transactions/transaction-form.tsx
   - Pass onSuccess callback to close dialog and invalidate dashboard queries

3. Update `apps/web/src/components/dashboard/dashboard-page-client.tsx`:
   - Add loans section below spending charts
   - Layout: grid of LoanOverviewCards
   - When card is clicked/expanded, show WhatIfSimulator for that loan
   - Add QuickAddFAB (rendered at page level, fixed position)
   - Per CONTEXT.md: "spending charts first, then loans section"
   - Final layout:
     1. MonthlySummary (income/expense/net)
     2. [SpendingPieChart | RecentTransactions] side by side
     3. SpendingTimeline (full width)
     4. Loans section header
     5. Grid of LoanOverviewCards
     6. When loan selected: WhatIfSimulator + AmortizationChart
     7. QuickAddFAB (fixed position, always visible)
  </action>
  <verify>
    - `cd apps/web && bun run typecheck` passes
    - `bun x ultracite check` passes
    - Slider responds without lag (useDeferredValue working)
    - FAB opens transaction form, form submits successfully
  </verify>
  <done>
    - What-if simulator shows real-time payoff changes as slider moves
    - Comparison shows months saved and interest saved
    - FAB is visible and opens full transaction form
    - Submitting via FAB creates transaction and updates dashboard
    - VIZ-04 and DASH-04 requirements complete
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd packages/api && bun run typecheck` - no type errors
2. `cd apps/web && bun run typecheck` - no type errors
3. `bun x ultracite check` - no lint errors
4. Dashboard shows loan section with cards
5. Clicking loan card expands what-if simulator
6. Slider is responsive (no lag)
7. What-if shows payoff date change, months saved, interest saved
8. FAB button visible, opens transaction form
9. Submitting transaction refreshes dashboard data
</verification>

<success_criteria>
- Loan cards display on dashboard with balance, payoff date, progress
- Amortization chart shows balance declining over time
- What-if simulator responds to slider in real-time (no lag)
- Simulator shows: new payoff date, months saved, interest saved
- FAB in bottom-right corner opens transaction form
- Transaction submission from FAB updates dashboard
- All Phase 5 requirements complete: TAG-04, VIZ-01, VIZ-02, VIZ-03, VIZ-04, DASH-01, DASH-02, DASH-03, DASH-04
</success_criteria>

<output>
After completion, create `.planning/phases/05-visualizations-dashboard/05-03-SUMMARY.md`
</output>
