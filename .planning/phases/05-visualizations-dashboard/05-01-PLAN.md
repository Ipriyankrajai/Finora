---
phase: 05-visualizations-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/src/hooks/use-dashboard.ts
  - apps/web/src/components/dashboard/monthly-summary.tsx
  - apps/web/src/components/dashboard/spending-pie-chart.tsx
  - apps/web/src/components/dashboard/recent-transactions.tsx
  - apps/web/src/components/dashboard/dashboard-page-client.tsx
  - apps/web/src/app/(authenticated)/dashboard/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can see this month's income, expenses, and net on dashboard"
    - "User can see spending breakdown pie chart by tag (top 5 + Other)"
    - "User can click pie segment to see transactions for that tag"
    - "User can see last 5 recent transactions on dashboard"
  artifacts:
    - path: "apps/web/src/hooks/use-dashboard.ts"
      provides: "Dashboard data fetching hook"
      exports: ["useDashboard"]
    - path: "apps/web/src/components/dashboard/monthly-summary.tsx"
      provides: "Income/expense/net summary cards"
      min_lines: 40
    - path: "apps/web/src/components/dashboard/spending-pie-chart.tsx"
      provides: "Recharts pie chart with click handler"
      min_lines: 60
    - path: "apps/web/src/components/dashboard/dashboard-page-client.tsx"
      provides: "Dashboard page orchestration"
      min_lines: 50
  key_links:
    - from: "apps/web/src/components/dashboard/dashboard-page-client.tsx"
      to: "useDashboard hook"
      via: "import and call"
      pattern: "useDashboard\\(\\)"
    - from: "apps/web/src/hooks/use-dashboard.ts"
      to: "trpc.dashboard.summary"
      via: "tRPC query"
      pattern: "trpc\\.dashboard\\.summary"
    - from: "apps/web/src/components/dashboard/spending-pie-chart.tsx"
      to: "recharts"
      via: "import"
      pattern: "from.*recharts"
---

<objective>
Create the dashboard foundation with monthly summary widgets, spending pie chart, and recent transactions display.

Purpose: Establishes the core dashboard view where users see their financial overview at a glance. The pie chart enables TAG-04 (spending totals by tag) and VIZ-01 (spending breakdown chart). Monthly summary fulfills DASH-01.

Output: Working dashboard page with summary cards, interactive pie chart with click-to-expand, and recent transactions list.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visualizations-dashboard/05-CONTEXT.md
@.planning/phases/05-visualizations-dashboard/05-RESEARCH.md

# Key existing files
@packages/api/src/routers/dashboard.ts
@apps/web/src/lib/format.ts
@apps/web/src/hooks/use-loans.ts
@apps/web/src/components/transactions/transaction-row.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install recharts and create dashboard hooks</name>
  <files>
    apps/web/package.json
    apps/web/src/hooks/use-dashboard.ts
  </files>
  <action>
1. Install recharts in apps/web:
   ```bash
   cd apps/web && bun add recharts
   ```

2. Create `apps/web/src/hooks/use-dashboard.ts`:
   - Export `useDashboard()` hook that calls `trpc.dashboard.summary.queryOptions()`
   - Return structured data: `{ monthlySummary, topTags, otherTagsTotal, loanOverview, isLoading, error }`
   - Follow the pattern from use-loans.ts (use useQuery with queryOptions)

3. The hook should type the response properly:
   ```typescript
   interface MonthlySummary {
     incomeCents: bigint;
     expenseCents: bigint;
     netCents: bigint;
   }
   interface TagSpending {
     tagId: string;
     tagName: string;
     tagColor: string;
     totalCents: bigint;
   }
   interface LoanOverview {
     id: string;
     name: string;
     balanceCents: bigint;
     interestPaidCents: bigint;
     projectedPayoffDate: Date;
     totalInterestRemainingCents: bigint;
   }
   ```
  </action>
  <verify>
    - `bun install` completes without errors in apps/web
    - TypeScript compiles: `cd apps/web && bun run typecheck`
  </verify>
  <done>
    - recharts is in package.json dependencies
    - useDashboard hook exports correctly typed dashboard data
  </done>
</task>

<task type="auto">
  <name>Task 2: Create monthly summary and spending pie chart components</name>
  <files>
    apps/web/src/components/dashboard/monthly-summary.tsx
    apps/web/src/components/dashboard/spending-pie-chart.tsx
  </files>
  <action>
1. Create `apps/web/src/components/dashboard/monthly-summary.tsx`:
   - Accept `{ incomeCents, expenseCents, netCents }` props
   - Render 3 cards in a responsive grid (1 col mobile, 3 cols desktop)
   - Each card shows label (Income/Expenses/Net) and formatted amount
   - Use formatCents from @/lib/format
   - Apply green color for income, red for expenses, appropriate color for net based on positive/negative
   - Use Card component from @/components/ui/card

2. Create `apps/web/src/components/dashboard/spending-pie-chart.tsx`:
   - Accept `{ topTags, otherTagsTotal, onTagClick }` props
   - Import from recharts: ResponsiveContainer, PieChart, Pie, Cell, Tooltip
   - Convert BigInt to number for chart: `Number(totalCents)` (per RESEARCH.md pitfall)
   - Only include "Other" segment if `otherTagsTotal > 0n`
   - Use tag colors from data for Cell fill
   - Create custom Tooltip component showing tagName and formatted amount
   - Handle click on segments: call `onTagClick(tagId)` or `onTagClick("other")`
   - Set parent div height explicitly: `<div className="h-[300px]">` (per RESEARCH.md pitfall)
   - Show friendly empty state if no data: "No spending recorded this month" with FileQuestion icon
   - Make pie segments clickable with cursor: pointer style
  </action>
  <verify>
    - TypeScript compiles: `cd apps/web && bun run typecheck`
    - No lint errors: `bun x ultracite check`
  </verify>
  <done>
    - MonthlySummary renders 3 cards with formatted currency
    - SpendingPieChart renders Recharts pie with click handlers
    - Empty state displays when no spending data
  </done>
</task>

<task type="auto">
  <name>Task 3: Create recent transactions and dashboard page</name>
  <files>
    apps/web/src/components/dashboard/recent-transactions.tsx
    apps/web/src/components/dashboard/dashboard-page-client.tsx
    apps/web/src/app/(authenticated)/dashboard/page.tsx
  </files>
  <action>
1. Create `apps/web/src/components/dashboard/recent-transactions.tsx`:
   - Accept `{ transactions }` prop (array of recent transactions)
   - For now, fetch transactions via trpc.transaction.list with limit: 5
   - Create internal hook or useQuery call within component
   - Display each transaction with: date, description/notes, amount (colored by type), tags
   - Reuse TransactionRow styling patterns or create simplified version
   - Show "No transactions yet" empty state with friendly message
   - Add "View all" link to /transactions page

2. Create `apps/web/src/components/dashboard/dashboard-page-client.tsx`:
   - "use client" directive at top
   - Call useDashboard() hook
   - Manage `expandedTag` state for pie chart click-to-expand
   - Layout structure per CONTEXT.md:
     - Top: MonthlySummary (income/expense/net)
     - Below: Two-column grid on desktop (pie chart left, recent transactions right)
     - When a tag is clicked, show filtered transactions inline below the pie chart
   - Handle loading state with skeletons
   - Handle error state with error message

3. Create/update `apps/web/src/app/(authenticated)/dashboard/page.tsx`:
   - Import and render DashboardPageClient
   - Set appropriate page metadata/title if needed
  </action>
  <verify>
    - `cd apps/web && bun run typecheck` passes
    - `cd apps/web && bun run dev` starts without errors
    - Navigate to /dashboard - page renders with summary, pie chart, recent transactions
  </verify>
  <done>
    - Dashboard page shows monthly income/expense/net cards
    - Pie chart renders with tag spending breakdown
    - Clicking pie segment shows transactions for that tag
    - Recent transactions list shows last 5 transactions
    - Empty states display appropriately when no data
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd apps/web && bun run typecheck` - no type errors
2. `bun x ultracite check` - no lint errors
3. Dev server runs and dashboard page loads
4. Pie chart is interactive (hover shows tooltip, click expands tag transactions)
5. All monetary values display with proper currency formatting
</verification>

<success_criteria>
- Dashboard page accessible at /dashboard
- Monthly summary shows income, expenses, net for current month
- Spending pie chart shows top 5 tags + "Other" with correct colors
- Clicking pie segment inline-expands to show transactions for that tag
- Recent transactions widget shows last 5 transactions
- Empty states are friendly (not just "No data")
- Requirements covered: DASH-01, DASH-02, TAG-04, VIZ-01 (partial)
</success_criteria>

<output>
After completion, create `.planning/phases/05-visualizations-dashboard/05-01-SUMMARY.md`
</output>
