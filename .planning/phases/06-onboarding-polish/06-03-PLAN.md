---
phase: 06-onboarding-polish
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/web/src/app/(dashboard)/dashboard/settings/page.tsx
  - apps/web/src/components/settings/profile-form.tsx
  - apps/web/src/components/settings/account-section.tsx
  - apps/web/src/components/settings/delete-account-dialog.tsx
  - apps/web/src/components/empty-states/empty-state.tsx
  - apps/web/src/components/empty-states/dashboard-empty.tsx
  - apps/web/src/components/empty-states/transactions-empty.tsx
  - apps/web/src/components/empty-states/loans-empty.tsx
  - apps/web/src/components/empty-states/tags-empty.tsx
  - apps/web/src/components/dashboard/dashboard-page-client.tsx
  - apps/web/src/app/(dashboard)/dashboard/transactions/page.tsx
  - apps/web/src/app/(dashboard)/dashboard/loans/page.tsx
  - apps/web/src/components/tags/tag-list.tsx
autonomous: true

must_haves:
  truths:
    - "User can update display name in settings and see it reflected"
    - "User can select preferred currency symbol from dropdown in settings"
    - "User can delete their account with confirmation dialog"
    - "User can replay onboarding from settings"
    - "Dashboard shows guided checklist empty state when no data exists"
    - "Transactions page shows friendly empty state with CTA when no transactions"
    - "Loans page shows friendly empty state with CTA when no loans"
    - "Tags sidebar section shows empty state with CTA when no tags"
  artifacts:
    - path: "apps/web/src/components/settings/profile-form.tsx"
      provides: "Name and currency edit form using TanStack Form"
      min_lines: 40
    - path: "apps/web/src/components/settings/delete-account-dialog.tsx"
      provides: "Confirmation dialog for account deletion"
      min_lines: 30
    - path: "apps/web/src/components/empty-states/empty-state.tsx"
      provides: "Reusable empty state component with icon, title, description, CTA"
      exports: ["EmptyState"]
    - path: "apps/web/src/components/empty-states/dashboard-empty.tsx"
      provides: "Dashboard-specific guided checklist empty state"
      min_lines: 30
  key_links:
    - from: "apps/web/src/components/settings/profile-form.tsx"
      to: "trpc.user.updateProfile + trpc.user.updateCurrency"
      via: "useUpdateProfile and useUpdateCurrency hooks"
      pattern: "updateProfile|updateCurrency"
    - from: "apps/web/src/components/settings/delete-account-dialog.tsx"
      to: "authClient.deleteUser"
      via: "better-auth client SDK"
      pattern: "authClient\\.deleteUser"
    - from: "apps/web/src/components/dashboard/dashboard-page-client.tsx"
      to: "apps/web/src/components/empty-states/dashboard-empty.tsx"
      via: "Conditional render when dashboard data is empty"
      pattern: "DashboardEmpty|dashboard-empty"
---

<objective>
Expand the settings page with profile editing (name, currency), account management (sign out, delete account, replay onboarding), and add tailored empty states to all main pages.

Purpose: Settings satisfies SETT-01 (update display name) and SETT-02 (set currency symbol). Empty states ensure new users see helpful guidance on every page when no data exists, completing the onboarding experience.
Output: Full settings page with profile form, delete account dialog, and 5 empty state components integrated into their respective pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-onboarding-polish/06-CONTEXT.md
@.planning/phases/06-onboarding-polish/06-RESEARCH.md
@.planning/phases/06-onboarding-polish/06-01-SUMMARY.md

Key references:
- apps/web/src/app/(dashboard)/dashboard/settings/page.tsx (existing settings to expand)
- apps/web/src/hooks/use-user-settings.ts (from 06-01, hooks for mutations)
- apps/web/src/lib/format.ts (CURRENCY_SYMBOLS constant from 06-01)
- apps/web/src/components/loans/loan-card.tsx (delete dialog pattern)
- apps/web/src/components/ui/dialog.tsx (dialog component)
- apps/web/src/components/ui/select.tsx (select component for currency)
- apps/web/src/components/dashboard/dashboard-page-client.tsx (add empty state)
- apps/web/src/app/(dashboard)/dashboard/transactions/page.tsx (add empty state)
- apps/web/src/app/(dashboard)/dashboard/loans/page.tsx (add empty state)
- apps/web/src/components/tags/tag-list.tsx (add empty state)
- apps/web/src/lib/auth-client.ts (authClient for deleteUser)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings page with profile form, currency, account management</name>
  <files>
    apps/web/src/app/(dashboard)/dashboard/settings/page.tsx
    apps/web/src/components/settings/profile-form.tsx
    apps/web/src/components/settings/account-section.tsx
    apps/web/src/components/settings/delete-account-dialog.tsx
  </files>
  <action>
    1. **Create ProfileForm** at `apps/web/src/components/settings/profile-form.tsx`:
       - "use client" component.
       - Uses `useUserSettings()` to get current name and currencySymbol.
       - Uses TanStack Form with Zod validation: name (min 2, max 100), currencySymbol (from CURRENCY_SYMBOLS values).
       - Two fields:
         - Display Name: text Input component, pre-filled from settings.
         - Currency Symbol: Select component from `@/components/ui/select.tsx` with options from CURRENCY_SYMBOLS constant.
       - Submit button: "Save Changes". On submit, call `updateProfile.mutateAsync({ name })` and `updateCurrency.mutateAsync({ currencySymbol })`, then `router.refresh()` to update server-rendered session data (sidebar shows name from server session). Show toast.success.
       - Loading state on submit button.
       - Follow the form pattern from transaction-form.tsx (TanStack Form + Zod validators.onSubmit).

    2. **Create AccountSection** at `apps/web/src/components/settings/account-section.tsx`:
       - "use client" component.
       - Shows email (read-only display from session).
       - "Sign Out" button (existing logic from current settings page).
       - "Replay Onboarding" link — a text button/link that navigates to `/onboarding`. Before navigating, call a tRPC mutation to set hasCompletedOnboarding=false (create a small inline mutation or add a `resetOnboarding` endpoint to user router — simplest is to just navigate to /onboarding since the wizard sets the flag on completion; but the dashboard redirect won't trigger if flag is true. So: use a simple approach — call prisma.user.update to set false via a new tRPC mutation `user.resetOnboarding`, OR simpler: just set window.location to /onboarding and have the wizard work regardless of current flag state).

       **Simplest approach**: Add the onboarding page to NOT require hasCompletedOnboarding=false — it works for any authenticated user. The "Replay Onboarding" link simply navigates to `/onboarding`. The wizard always works and sets the flag to true on completion. This means no extra mutation needed for replay.

       - "Delete Account" red button at bottom — opens DeleteAccountDialog.

    3. **Create DeleteAccountDialog** at `apps/web/src/components/settings/delete-account-dialog.tsx`:
       - Uses Dialog component from `@/components/ui/dialog.tsx`.
       - Warning text about permanent data loss (all transactions, tags, loans will be deleted).
       - Confirmation: require typing "DELETE" or similar to enable the delete button.
       - On confirm: call `authClient.deleteUser()` with fetchOptions.onSuccess that redirects to "/" and shows toast. On error, show toast.error.
       - Follow the delete confirmation pattern from loan payment deletion (apps/web/src/components/loans/ area).
       - If better-auth deleteUser has Prisma v7 issues (like updateUser), fall back to a tRPC mutation. Test the better-auth approach first since research was uncertain about deleteUser being affected.

    4. **Rewrite settings page** at `apps/web/src/app/(dashboard)/dashboard/settings/page.tsx`:
       - "use client" component.
       - Header: "Settings" h1 with description.
       - Two Card sections:
         - **Profile** card: renders `<ProfileForm />`.
         - **Account** card: renders `<AccountSection />`.
       - Remove the old inline profile display and sign-out button (replaced by new components).

    5. **Run lint/format**: `bun x ultracite fix`.
  </action>
  <verify>
    - `bun x ultracite check` passes
    - `bun run build` succeeds
    - Settings page renders ProfileForm and AccountSection
    - ProfileForm has name input and currency select
    - DeleteAccountDialog exists with confirmation flow
  </verify>
  <done>
    Settings page has editable profile form (name + currency dropdown) using TanStack Form. Account section has sign out, replay onboarding link, and delete account with confirmation dialog. All mutations use tRPC user router (not better-auth updateUser). User can update name (SETT-01) and currency symbol (SETT-02).
  </done>
</task>

<task type="auto">
  <name>Task 2: Empty states for all main pages</name>
  <files>
    apps/web/src/components/empty-states/empty-state.tsx
    apps/web/src/components/empty-states/dashboard-empty.tsx
    apps/web/src/components/empty-states/transactions-empty.tsx
    apps/web/src/components/empty-states/loans-empty.tsx
    apps/web/src/components/empty-states/tags-empty.tsx
    apps/web/src/components/dashboard/dashboard-page-client.tsx
    apps/web/src/app/(dashboard)/dashboard/transactions/page.tsx
    apps/web/src/app/(dashboard)/dashboard/loans/page.tsx
    apps/web/src/components/tags/tag-list.tsx
  </files>
  <action>
    1. **Create reusable EmptyState component** at `apps/web/src/components/empty-states/empty-state.tsx`:
       - Props: `icon` (ReactNode), `title` (string), `description` (string), `action` (optional: { label: string, onClick?: () => void, href?: string }).
       - Layout: centered flex column with padding. Icon in a rounded circle with primary/10 background. Title as h3, description as muted text. Action renders as Button (onClick) or Link (href).
       - Follow the styling pattern from existing empty patterns in the codebase (transaction list, loan list have inline empty states).
       - Export as named export.

    2. **Create DashboardEmpty** at `apps/web/src/components/empty-states/dashboard-empty.tsx`:
       - Special guided empty state for dashboard — NOT just icon+text.
       - Card-based checklist showing setup progress:
         - "Account created" (always checked, green checkmark)
         - "Add your first tag" (link to /dashboard/settings or open tag form — check if tags exist via props or query)
         - "Record a transaction" (link to /dashboard/transactions)
         - "Track a loan" (link to /dashboard/loans)
       - Each item: checkbox/circle icon (filled if done, empty if not) + label + link.
       - Headline: "Welcome to Finora! Let's get started."
       - Description: "Complete these steps to set up your financial dashboard."
       - Pass counts as props (tagCount, transactionCount, loanCount) to determine which steps are complete.

    3. **Create TransactionsEmpty** at `apps/web/src/components/empty-states/transactions-empty.tsx`:
       - Uses EmptyState component.
       - Icon: Wallet from lucide-react.
       - Title: "No transactions yet"
       - Description: "Start tracking your income and expenses to see where your money goes."
       - Action: { label: "Add Transaction", onClick } — the onClick should trigger the transaction form (pass as prop from parent).

    4. **Create LoansEmpty** at `apps/web/src/components/empty-states/loans-empty.tsx`:
       - Uses EmptyState component.
       - Icon: CreditCard from lucide-react.
       - Title: "No loans yet"
       - Description: "Add your loans to track payments, see remaining balances, and project payoff dates."
       - Action: { label: "Add Loan", onClick } — onClick triggers loan form (pass as prop).

    5. **Create TagsEmpty** at `apps/web/src/components/empty-states/tags-empty.tsx`:
       - Uses EmptyState component.
       - Icon: Tag from lucide-react.
       - Title: "No tags yet"
       - Description: "Create tags to categorize your transactions."
       - Action: { label: "Create Tag", onClick } — onClick triggers tag form (pass as prop).

    6. **Integrate DashboardEmpty** into `apps/web/src/components/dashboard/dashboard-page-client.tsx`:
       - Check if dashboard data is empty (no transactions, no loans, no tags — all counts are 0 or data arrays are empty).
       - If empty, render `<DashboardEmpty />` instead of the normal dashboard content.
       - Pass tag/transaction/loan counts to DashboardEmpty for checklist state.

    7. **Integrate TransactionsEmpty** into transaction list/page:
       - In the transactions page component, when the transaction list query returns 0 results AND no filters are active, show `<TransactionsEmpty />`.
       - If filters are active but no results, show a different message ("No transactions match your filters").

    8. **Integrate LoansEmpty** into loans page:
       - In the loans page, when the loan list is empty, show `<LoansEmpty />` instead of the loan grid.

    9. **Integrate TagsEmpty** into tag list sidebar:
       - In `apps/web/src/components/tags/tag-list.tsx`, when no tags exist, show a small empty state inside the sidebar section (compact version — just text + small "Create" link, since sidebar space is limited).

    10. **Run lint/format**: `bun x ultracite fix`.
  </action>
  <verify>
    - `bun x ultracite check` passes
    - `bun run build` succeeds
    - EmptyState component exists and is reusable
    - DashboardEmpty shows checklist with setup steps
    - TransactionsEmpty, LoansEmpty, TagsEmpty exist
    - Dashboard page conditionally renders DashboardEmpty when no data
    - Transaction, Loan pages render empty states when lists are empty
    - Tag list shows empty state in sidebar when no tags
  </verify>
  <done>
    Reusable EmptyState component created. Dashboard gets guided checklist empty state. Transactions, Loans, and Tags pages get tailored empty states with CTAs. Empty states show when no data exists and provide clear next-step guidance for new users.
  </done>
</task>

</tasks>

<verification>
1. `bun x ultracite check` passes
2. `bun run build` succeeds
3. Settings page has profile form with name and currency dropdown
4. Settings page has account section with sign out, replay onboarding, delete account
5. Delete account has confirmation dialog
6. Dashboard shows guided checklist when empty
7. Transactions page shows empty state when no transactions
8. Loans page shows empty state when no loans
9. Tags sidebar shows empty state when no tags
10. All empty states have relevant icons, descriptions, and CTAs
</verification>

<success_criteria>
- User can update display name in settings (SETT-01)
- User can set preferred currency symbol from dropdown (SETT-02)
- User can delete account with confirmation
- User can replay onboarding from settings
- Dashboard shows guided setup checklist when no data exists
- All main pages (transactions, loans, tags) show friendly empty states with CTAs
- Empty states guide users to take their first action
</success_criteria>

<output>
After completion, create `.planning/phases/06-onboarding-polish/06-03-SUMMARY.md`
</output>
