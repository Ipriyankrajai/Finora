---
phase: 06-onboarding-polish
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/web/src/app/(onboarding)/layout.tsx
  - apps/web/src/app/(onboarding)/onboarding/page.tsx
  - apps/web/src/components/onboarding/onboarding-wizard.tsx
  - apps/web/src/components/onboarding/step-welcome.tsx
  - apps/web/src/components/onboarding/step-expenses.tsx
  - apps/web/src/components/onboarding/step-loans.tsx
  - apps/web/src/components/onboarding/step-setup.tsx
  - apps/web/src/app/(dashboard)/layout.tsx
autonomous: true

must_haves:
  truths:
    - "New user (hasCompletedOnboarding=false) is redirected from dashboard to /onboarding"
    - "Onboarding wizard shows 4 steps: Welcome, Expenses, Loans, Setup"
    - "Welcome step introduces Finora and its value proposition"
    - "Expenses step previews the expense tracking feature with visual mockup"
    - "Loans step previews the loan management feature with visual mockup"
    - "Setup step collects display name and currency symbol preference"
    - "Completing onboarding sets hasCompletedOnboarding=true and redirects to dashboard"
    - "Skip option available on every step, also completes onboarding"
  artifacts:
    - path: "apps/web/src/app/(onboarding)/layout.tsx"
      provides: "Clean layout without dashboard chrome for onboarding flow"
      min_lines: 15
    - path: "apps/web/src/app/(onboarding)/onboarding/page.tsx"
      provides: "Onboarding page entry point"
      min_lines: 5
    - path: "apps/web/src/components/onboarding/onboarding-wizard.tsx"
      provides: "Multi-step wizard with state management and step transitions"
      min_lines: 50
    - path: "apps/web/src/components/onboarding/step-setup.tsx"
      provides: "Name and currency collection form"
      min_lines: 30
  key_links:
    - from: "apps/web/src/app/(dashboard)/layout.tsx"
      to: "/onboarding"
      via: "Server-side redirect when hasCompletedOnboarding is false"
      pattern: "redirect.*onboarding"
    - from: "apps/web/src/components/onboarding/onboarding-wizard.tsx"
      to: "trpc.user.completeOnboarding"
      via: "useCompleteOnboarding hook"
      pattern: "completeOnboarding"
    - from: "apps/web/src/components/onboarding/step-setup.tsx"
      to: "trpc.user.updateProfile + trpc.user.updateCurrency"
      via: "useUpdateProfile and useUpdateCurrency hooks"
      pattern: "updateProfile|updateCurrency"
---

<objective>
Build a 4-step onboarding wizard that introduces new users to Finora's expense tracking and loan management features, collects their name and currency preference, and redirects to the dashboard upon completion.

Purpose: New users should understand Finora's value proposition (expense tracking + loan payoff) before landing on the dashboard, ensuring they know what the app does and how to get started. This satisfies ONBD-01 and ONBD-02.
Output: Onboarding route group with clean layout, 4-step wizard, and dashboard redirect logic.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-onboarding-polish/06-CONTEXT.md
@.planning/phases/06-onboarding-polish/06-RESEARCH.md
@.planning/phases/06-onboarding-polish/06-01-SUMMARY.md

Key references:
- apps/web/src/app/(dashboard)/layout.tsx (add onboarding redirect)
- apps/web/src/app/(auth)/layout.tsx (clean layout pattern to follow)
- apps/web/src/app/(auth)/sign-up/page.tsx (step transition animation pattern)
- apps/web/src/hooks/use-user-settings.ts (from 06-01, hooks for mutations)
- apps/web/src/lib/format.ts (CURRENCY_SYMBOLS constant from 06-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Onboarding route group and dashboard redirect</name>
  <files>
    apps/web/src/app/(onboarding)/layout.tsx
    apps/web/src/app/(onboarding)/onboarding/page.tsx
    apps/web/src/app/(dashboard)/layout.tsx
  </files>
  <action>
    1. **Create (onboarding) route group layout** at `apps/web/src/app/(onboarding)/layout.tsx`:
       - Server component (no "use client").
       - Check session via `auth.api.getSession({ headers: await headers() })`. If no session, redirect to `/sign-in`.
       - Do NOT check hasCompletedOnboarding here — this avoids redirect loops.
       - Return minimal layout: centered container with subtle background, no sidebar/header.
       - Follow the (auth) layout pattern for structure but use a wider container suitable for wizard content.

    2. **Create onboarding page** at `apps/web/src/app/(onboarding)/onboarding/page.tsx`:
       - Simple page that renders `<OnboardingWizard />`.
       - Export metadata: `{ title: "Get Started | Finora" }`.

    3. **Add onboarding redirect** to `apps/web/src/app/(dashboard)/layout.tsx`:
       - After the session check (and before rendering), query the user's `hasCompletedOnboarding` flag.
       - Use `prisma.user.findUnique({ where: { id: session.user.id }, select: { hasCompletedOnboarding: true } })`. Import prisma from `@finora2/db`.
       - If `!user?.hasCompletedOnboarding`, call `redirect("/onboarding")`.
       - Place this check AFTER the auth redirect (so unauthenticated users go to sign-in, not onboarding).

    **IMPORTANT**: The (onboarding) layout must NOT check hasCompletedOnboarding. Only the (dashboard) layout checks it and redirects. This prevents redirect loops.
  </action>
  <verify>
    - `bun run build` succeeds (confirms both layouts and page compile)
    - Verify (onboarding) route group exists with layout.tsx and onboarding/page.tsx
    - Verify (dashboard) layout.tsx contains prisma.user.findUnique and redirect("/onboarding") logic
  </verify>
  <done>
    (onboarding) route group created with clean layout (auth check only, no onboarding check). Dashboard layout redirects users with hasCompletedOnboarding=false to /onboarding. No redirect loop possible.
  </done>
</task>

<task type="auto">
  <name>Task 2: Onboarding wizard with 4 steps and CSS transitions</name>
  <files>
    apps/web/src/components/onboarding/onboarding-wizard.tsx
    apps/web/src/components/onboarding/step-welcome.tsx
    apps/web/src/components/onboarding/step-expenses.tsx
    apps/web/src/components/onboarding/step-loans.tsx
    apps/web/src/components/onboarding/step-setup.tsx
  </files>
  <action>
    1. **Create OnboardingWizard** at `apps/web/src/components/onboarding/onboarding-wizard.tsx`:
       - "use client" component.
       - State: `currentStep` using useState, from steps array: `["welcome", "expenses", "loans", "setup"] as const`.
       - Progress indicator: 4 dots/circles at the top showing current step (filled for completed + current, empty for upcoming). Use Tailwind for styling.
       - Step transitions: CSS transitions following the pattern from sign-up page. When advancing, current step slides out left + fades, new step slides in from right + fades in. Use `transition-all duration-500` with translate and opacity classes.
       - "Skip" text link in top-right corner that calls handleSkip (completes onboarding, redirects to dashboard).
       - handleNext function: advances to next step. On the last step ("setup"), the setup step handles completion directly.
       - handleSkip function: calls `completeOnboarding.mutateAsync()` then `router.push("/dashboard")`.
       - Import hooks from `use-user-settings.ts` (useCompleteOnboarding).
       - Render the appropriate step component based on currentStep, passing onNext and relevant props.

    2. **Create StepWelcome** at `apps/web/src/components/onboarding/step-welcome.tsx`:
       - Large Finora logo or app name at top.
       - Headline: "Welcome to Finora" (or similar friendly greeting).
       - Brief description: what Finora helps with (track expenses, manage loans, see where money goes).
       - Visual: use Lucide icons (Wallet, TrendingUp, PieChart) in a row/grid to represent features.
       - CTA button: "Get Started" calls onNext.
       - Keep copy concise and friendly.

    3. **Create StepExpenses** at `apps/web/src/components/onboarding/step-expenses.tsx`:
       - Headline: "Track Your Spending" (or similar).
       - Description: explain expense tracking with tags, filtering, and visual breakdowns.
       - Visual mockup: a stylized card showing a mini transaction list with colored tag dots (static/fake data). Use the project's Card, Badge-like tag chips to make it feel real.
       - CTA: "Next" button calls onNext.

    4. **Create StepLoans** at `apps/web/src/components/onboarding/step-loans.tsx`:
       - Headline: "Manage Your Loans" (or similar).
       - Description: explain loan tracking, payment logging, payoff projections, and what-if simulation.
       - Visual mockup: a stylized card showing a loan card with balance, progress bar, and a mini chart/graph shape (static). Use project's Card component.
       - CTA: "Next" button calls onNext.

    5. **Create StepSetup** at `apps/web/src/components/onboarding/step-setup.tsx`:
       - Headline: "Set Up Your Profile".
       - Form with 2 fields:
         - Display name: text input, pre-filled from session if available (pass user name as prop or use useUserSettings hook).
         - Currency symbol: Select dropdown using the CURRENCY_SYMBOLS constant from format.ts. Use the project's Select component from `@/components/ui/select.tsx`.
       - Use TanStack Form with Zod validation (name min 2 chars, currencySymbol min 1 max 3).
       - On submit: call updateProfile + updateCurrency + completeOnboarding mutations sequentially, then router.push("/dashboard").
       - CTA: "Finish Setup" button.
       - Show loading state during submission.

    6. **Run lint/format**: `bun x ultracite fix`.
  </action>
  <verify>
    - `bun x ultracite check` passes
    - `bun run build` succeeds
    - All 5 component files exist in apps/web/src/components/onboarding/
    - OnboardingWizard manages step state and renders step components
    - StepSetup has form with name + currency fields and calls mutations on submit
  </verify>
  <done>
    4-step onboarding wizard with CSS transitions between steps. Welcome introduces the app, Expenses previews spending tracking, Loans previews loan management, Setup collects name and currency. Skip available on all steps. Completing onboarding (via finish or skip) sets flag and redirects to dashboard.
  </done>
</task>

</tasks>

<verification>
1. `bun x ultracite check` passes
2. `bun run build` succeeds
3. Dashboard layout redirects to /onboarding when hasCompletedOnboarding=false
4. /onboarding page renders 4-step wizard
5. Progress indicator shows current step
6. Steps transition with CSS animations
7. Skip button available on all steps
8. Setup step collects name and currency via form
9. Completing onboarding calls completeOnboarding mutation and redirects to /dashboard
10. No redirect loop between dashboard and onboarding
</verification>

<success_criteria>
- New user (hasCompletedOnboarding=false) is redirected to onboarding wizard (ONBD-01, ONBD-02)
- Wizard has 4 steps: Welcome, Expenses, Loans, Setup
- Expense tracking features are previewed (ONBD-01)
- Loan management features are previewed (ONBD-02)
- User can set display name and currency in setup step
- Skip option bypasses wizard and completes onboarding
- All steps have smooth CSS transitions
- Completing wizard redirects to dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/06-onboarding-polish/06-02-SUMMARY.md`
</output>
